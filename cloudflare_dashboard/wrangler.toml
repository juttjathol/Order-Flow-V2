name = "order-flow-saas"
pages_build_output_dir = "public"
compatibility_date = "2025-10-01"

# Bind D1 in the Cloudflare dashboard (no id needed here):
# Pages project → Settings → Bindings → D1 database
#   Variable name: DB
#   Database: order_flow
#
# Secrets (Settings → Environment variables):
#   ADMIN_PASSWORD
#   ADMIN_SECRET
